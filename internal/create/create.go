package create

import "github.com/thaffenden/pkb/internal/config"

// Create creates the document as per the template config.
func Create(templateConf config.Template, subTemplate config.Template, name string) error {
	// check if parent dirs exist
	// if not create them
	// sanitise file name
	// add id to name
	// create file
	return nil
}
